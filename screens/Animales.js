import React from 'react'
import {View, Text, StyleSheet} from 'react-native'

const Animales = () => {
    return (
        <View>
            <Text>Listado de animales</Text>
        </View>
    )


const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#cdcdcd',
        alignItems: 'center',
        justifyContent: 'center',
    },
})}

export default Animales;